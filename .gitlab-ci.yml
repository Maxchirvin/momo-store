stages:
  - test
  - build

test_stage:
  stage: test
  script:
    - echo "Testing Momo Store application"
    - echo "Pipeline ID: ${CI_PIPELINE_ID}"

build_stage:
  stage: build
  script:
    - echo "Building Momo Store application"
    - echo "Build completed successfully"
